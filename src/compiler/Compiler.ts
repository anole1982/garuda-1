import {Exception} from '../exception';
import {CompilerOptions} from "./CompilerOptions";
export class Compiler {
    private guid:number;
    private opcodes:any[];
    private children:any[];
    private sourceNode = [];
    private options:CompilerOptions;

    public compile(html:string, options:CompilerOptions){

    }

}